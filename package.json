{
  "name": "@automation3000/s7-knx",
  "version": "2.2.0",
  "description": "S7-KNX Gateway",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "watch": "nodemon src/index.js",
    "vminor": "npm version minor && git push && git push --tags",
    "vpatch": "npm version patch && git push && git push --tags",
    "vmajor": "npm version major && git push && git push --tags"
  },
  "author": "Automation 3000 SA",
  "license": "MIT",
  "dependencies": {
    "async-exit-hook": "^2.0.1",
    "debug": "^4.4.1",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "knx": "^2.5.4",
    "node-snap7": "^1.0.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
